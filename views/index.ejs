<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
  </head>
  <body>
    <ul>
      <% for( let index = 0; index < messages.length; index++ ) { %>
      <li>
        <div>
          User: <%= messages[index].user %> Text: <%= messages[index].text %>
          Date: <%= messages[index].added %>
        </div>
      </li>
      <% } %>
    </ul>
  </body>
</html>
